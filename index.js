const gameBoard = document.getElementById("game-board");
const player = "X";
function playerTurn() {
    if
}

function resetGameboard(parent){
    while(parent.firstChild) {
        parent.removeChild(parent.firstChild);
    }
}